# L4 渠道与订阅线 · 全量回归测试报告

> 2026-09-17 · 双层验证：既有 IT 深回归 + 新增 Playwright E2E（真实 fat jar 实例取证）

## 一、范围

CHN-001~005（自注册 SSRF 白名单+加签验证、verify/启停/删除）、ACH 公共渠道隔离、
SUB-001/002（订阅矩阵三段结构、全量替换、强制集校验）、quiet_hours 配置面

## 二、结果矩阵

| 层 | 载体 | 结果 |
|---|---|---|
| IT 深回归 | NfyChannelFlowTest + NfyChannelAdminTest + NfySubscriptionFlowTest | **20/20 绿**（RC=0） |
| Playwright E2E | l4-channels-subscriptions.spec.ts（13 用例） | **13/13 绿**（20 例含 L5 合跑 48s） |

| # | 用例 | 结果 |
|---|---|---|
| L4-01 | 自注册成功（PENDING 落库、不发验证消息） | ✅ |
| L4-02 | SSRF 四联拒绝 10609（http 明文/内网地址/非白名单域/非 443 端口） | ✅ |
| L4-03 | 非法类型/空 target/非法邮箱/名称超长 10100 | ✅ |
| L4-04 | 列表脱敏（`u***@`）+ verify 10604 + 未验证直启 10610 | ✅ |
| L4-05 | patch 改名/启停/非法枚举流转 | ✅ |
| L4-06 | 删除 + 防探测（删后同码操作 10400） | ✅ |
| L4-07 | ACH 公共渠道 scope=TENANT 三重隔离 + 管理 | ✅ |
| L4-08 | SUB-001 矩阵三段（types/available_channels INAPP 哨兵/items）+ 平台 PTE-005 联动 | ✅ |
| L4-09 | SUB-002 PUT 多类型×渠道：saved_count、回读一致、INAPP 恒首位、幂等、全量替换删行 | ✅ |
| L4-10 | 强制集 10606 / 无实例豁免 | ✅ |
| L4-11 | 越权/状态闸：10101 / 10601 / 10400（PENDING、他人渠道） | ✅ |
| L4-12 | quiet_hours 回显/缺省重置/4 类非法 10100 无半截落库 | ✅ |

## 三、发现与修复意见

| # | 级别 | 发现 | 修复意见 |
|---|---|---|---|
| 1 | P1（归 L5 线登记，ND-L5-01） | 渠道 **ENABLED 无 API 通路**：EMAIL verify 恒 10604 + patch ENABLED 恒 10610，纯 API 外发链断裂（详见 L5 报告） | ✅ **已修复（2026-09-18）**：EMAIL 可经 API 直启，本线 e2e 随全量复验 70/70 绿 |
| 2 | P3（环境） | **E-L4-01**：本机代理 fake-IP DNS 将白名单域（oapi.dingtalk.com 等）解析至 198.18.0.0/15（⊂SSRF 禁用 ULA 集）→ IM 渠道注册 10609。**产品判定按契约正确**（JVM 解析复核），生产直连 DNS 不受影响 | 部署文档标注 DNS 前置要求；证据 L4-01b-*.png |

SSRF 白名单在 runtime 自注册与 admin 公共渠道两面均生效（L4-02 / L7-05 交叉验证）。

## 四、结论

**L4 通过（附带 1 项 P1 使能性缺陷归口 L5）。** 校验矩阵、脱敏、防探测、矩阵全量替换与强制集语义全部符合契约。
