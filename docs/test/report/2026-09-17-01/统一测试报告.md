# notification4j 业务线验收回归 · 统一测试报告

> 2026-09-17 · 八条业务线 · 双层验证（既有 IT 深回归 + 新增 Playwright E2E 截图取证）
> **2026-09-18 修复轮**：P0×3 + P1×1 + P2×3 **全部修复并验收**（见 §八），修复后出厂等价实例全量复跑绿（单测 411 / IT 129 / E2E 70）

## 一、执行摘要

| 层面 | 结论 |
|---|---|
| **语义层（八线业务契约）** | ✅ **全部通过**。IT 129/129 + E2E 70/70，发送/幂等/公告回执/SSRF/订阅矩阵/引擎时序/生命周期状态机/前端握手逻辑全部符合契约 |
| **部署形态层（真实 fat jar 独立部署）** | ✅ ~~3 项 P0~~ **已修复**（D-1/D-2/D-3，2026-09-18）：出厂等价实例活体验证——SPA 未签名直调全通（D-1）、真实租户签名调用打通（D-2，NfySignatureFaceTest 防回归）、引擎 0 次 Wall 异常（D-3） |
| **使能通路层** | ✅ ~~1 项 P1~~ **已修复**（ND-L5-01，2026-09-18）：EMAIL 渠道可经 API 直启，纯 API 外发闭环恢复 |
| **前端体验层** | ✅ ~~2 项 P2~~ **已修复**（F-1/F-2，2026-09-18）：时间渲染正常、失效 token 出重连提示 |

一句话：**产品业务语义扎实（八线全绿）。出厂配置的三个装配/拦截缺陷曾使独立部署开箱即瘫（IT 环境签名关、无 Wall 而结构性不可见），2026-09-18 全部修复并在出厂等价实例上完成活体验收，当前缺陷台账清零（P3 观察项除外）。**

## 二、测试方法与环境

- **双层验证**：每条业务线先跑既有 IT 套件（Testcontainers PG16+Redis7 深回归），再对新编 Playwright E2E 场景跑绿（被测对象=真实 fat jar 独立部署实例）。
- **测试实例**：`notification4j-app-1.0.0.jar` + PG16/Redis7 独立容器（25432/26379）+ 引擎提速参数（scan 500ms/backoff 2,5,10s）+ SMTP sink（localhost:3925，node:net 自建，支持 454 故障注入）。
- **声明（测试轮 09-17 的出厂偏差，均已取证登记；三者在修复轮 09-18 已由产品侧修复取代，见 §八第二批）**：
  1. ~~实例关闭 `framework4j.signature.enabled`~~——D-1 修复后出厂 `path-patterns=[]`，测试实例已与出厂等价；
  2. ~~实例去除 Druid `wall` filter~~——D-3 修复后出厂 `filters: stat,slf4j`，同上；
  3. ~~L5 经 `helpers/pg-fixup.py` 绕过 ND-L5-01 造数~~——ND-L5-01 修复后 l5 spec 已改走真实 API 使能，pg-fixup 退出使用（保留备查）。
  现存旋钮仅为测试正当配置：数据源/Redis 端口指向独立容器、引擎提速参数、SMTP 目标。
- **取证**：API 用例响应渲染成证据页截图；UI 用例真实页面截图；共 **91 张**（`documents/test-report/screenshots/`；其中 **17 张前端界面截图已嵌入 L8 报告**，其余以文件形式索引）。

## 三、逐线结果矩阵

| 线 | IT 深回归 | Playwright E2E | 结论 | 报告 |
|---|---|---|---|---|
| L1 认证与开放接入 | 23/23（含 NfySignatureFaceTest×2 防回归） | 8/8 | ✅ 通过 | L1-认证线报告.md |
| L2 站内消息 | 21/21 | 6/6 | ✅ 通过 | L2-消息线报告.md |
| L3 公告 | 10/10 | 6/6 | ✅ 通过（第 28 步跨域修复点复验成立） | L3-公告线报告.md |
| L4 渠道与订阅 | 20/20 | 13/13 | ✅ 通过（附 P1 归口 L5，已修复） | L4-渠道订阅线报告.md |
| L5 外发引擎 | 15/15 | 7/7 | ✅ 通过（ND-L5-01 已修复，2026-09-18） | L5-引擎线报告.md |
| L6 平台治理与租户生命周期 | 17/17 | 10/10 | ✅ 通过 | L6-平台治理线报告.md |
| L7 Admin 运营治理 | 9/9 | 12/12 | ✅ 通过 | L7-Admin线报告.md |
| L8 前端消息中心 | 13/13 | 8/8（17 张界面截图嵌入报告） | ✅ 通过（F-1/F-2 已修复，2026-09-18） | L8-前端线报告.md |
| **合计** | **129/129** | **70/70** | — | — |

## 四、缺陷清单与修复意见（P0→P3，均未改代码）

### P0（独立部署形态开箱即瘫）——✅ 三项均已于 2026-09-18 修复并验收（见 §八）

| # | 缺陷 | 影响 | 修复 | 证据 |
|---|---|---|---|---|
| D-1 | ~~出厂 yml 对 `/nfy/api/v1/runtime/**` 强制 HMAC 签名，而消息中心 SPA 无签名层、也不可能持有租户 secret~~ | ~~形态一（前端入 jar）下消息中心所有页面不可用（10101）~~ | ✅ app yml `path-patterns: []`（SPA 与 S2S 共用路径，出厂摘除强制面；S2S 示例留注释）——出厂等价实例 e2e 70/70 即活体验证 | DEFECT-01-*.png（历史） |
| D-2 | ~~`NfyTenantSecretProvider` @Bean 与 fwk4j `SignatureAutoConfiguration` 无排序 → InMemory 兜底胜出~~ | ~~正确签名的调用全拒（10200）；形态三 client-starter 同瘫~~ | ✅ `@AutoConfigureBefore(name="fun.commons.framework4j.signature.config.SignatureAutoConfiguration")` + 去掉 `@ConditionalOnMissingBean`；新增 NfySignatureFaceTest 防回归（未签名 10101/真实租户签名 code=0） | NfySignatureFaceTest 红→绿 |
| D-3 | ~~出厂 Druid `filters` 默认含 `wall`，不识别 PG `FOR UPDATE OF d SKIP LOCKED`~~ | ~~外发引擎每次扫描必炸、投递永不发生（出厂实例 40 秒 32 次异常）~~ | ✅ 出厂 yml 显式 `filters: stat,slf4j` + 注释——出厂等价实例 Wall 异常 0 次 | DEFECT-03-*.png（历史） |

### P1

| # | 缺陷 | 影响 | 修复意见 | 证据 |
|---|---|---|---|---|
| ND-L5-01 | EMAIL 渠道 verify 恒 10604 + patch ENABLED 恒 10610（要求 last_verify_at），而计划/引擎只认 ENABLED | 纯 API 用户的 EMAIL 外发**闭环断裂**；IT 因 mapper 直置 ENABLED 未暴露（跨层盲区） | ① EMAIL verify 落地真校验；或 ② 首次投递时校验并自动 ENABLED（与 10604 文案一致化）；或 ③ 放宽 patch 前置 | L5-00-缺陷登记ND-L5-01.png |

### P2

| # | 缺陷 | 修复意见 | 证据 |
|---|---|---|---|
| D-4 | Redisson 端点不随 `framework4j.redis` host/port 覆盖，落默认 127.0.0.1:6379（RedissonConfig 仅 enabled 字段） | framework4j 侧复用 datasource host/port 或补 address 字段；出厂 yml 显式声明 | DEFECT-04-*.png |
| F-1 | 消息中心全站时间「Invalid Date」：后端 Long→String（雪花精度保护），前端 `new Date(string)` 不解析——Messages/Announcements/Bell 三处 | 前端统一 `new Date(Number(ts))` | L8-01/L8-03 截图 |
| F-2 | 无效/过期 token 呈现误导性空态：`notifyExpired`（401→重握手）全仓无调用方，数据失败静默成「暂无消息」 | client 层鉴权错误（401/403）回调触发重握手 + 页面错误态 | L8-08-*.png |

### P3（择要，详见各线报告）

| # | 线 | 发现 | 修复意见 |
|---|---|---|---|
| 1 | L3 | PAN 契约缺 stats 端点（confirm 核账仅经 detail 透出） | 补 PAN stats（与 AAN-005 同构） |
| 2 | L6/L7 | reset-secret / SEC 轮换后旧密 24h 宽限仍可用；测试注释「旧 secret 失效」失实 | 修注释；提供「立即失效」轮换选项（泄漏场景） |
| 3 | L6 | 单会话互踢：同租户重复 mintToken 撤销上一会话（10205），多服务共用凭据互踢 | 文档明示；或租户级 maxSessions 策略 |
| 4 | L7 | 直发模板渲染未接线（template_id=0 透传，渲染仅 preview；V1.0 边界内已登记） | 若宣称 V1.0 支持模板直发则接线 |
| 5 | L8 | 订阅页「站内信恒开」复选框渲染未勾选（裸 `model-value` 属性传空串） | 改 `:model-value="true"` |
| 6 | L8 | 铃铛下拉面板被嵌入 iframe 必然裁剪（DOM 断言全过，纯视觉） | 面板相对 iframe 视口定位或父页浮层承载 |
| 7 | L2/L7 | 共享实例回归自绊面：runtime 限流 10500、平台公告 20 条上限 10611、DLV 正向造数不可达 | 测试实例加限流白名单/测试渠道种子开关（CI 友好） |
| 8 | L4 | E-L4-01（环境）：本机代理 fake-IP DNS 使白名单域落入 SSRF 禁用集 → IM 注册 10609；产品判定按契约正确 | 部署文档标注 DNS 前置要求 |

## 五、证据索引

- `documents/test-report/screenshots/`：**91 张**。**L8 报告嵌入 17 张前端界面真实截图**（消息中心/五页/铃铛/握手拒绝/修复后重连提示）；其余为 API 证据页与缺陷取证页，按用例编号以文件形式在各线报告「证据」列引用
- `documents/test-report/it-logs/L1~L8.log`：IT 深回归原始日志（`it-summary.txt` 汇总 RC）
- `documents/test-report/e2e-results.json`：Playwright 结构化结果
- `documents/test-report/e2e-app.log`：被测实例日志

## 六、复跑手册

```bash
# 0. 基础设施（Docker Desktop 需运行）
docker start nfy4j-e2e-pg nfy4j-e2e-redis
# 1. 被测实例（fat jar + 容器 + 引擎提速 + SMTP 目标；detached）
bash frontend/e2e-regression/bin/start-app.sh          # http://localhost:9200
# 2. E2E 八线（宿主握手页 3000 由 global-setup 自动拉起）
cd frontend && npx playwright test -c e2e-regression/playwright.config.ts
# 3. IT 深回归八线（串行，防 m2 竞态）
bash frontend/e2e-regression/bin/run-it-lines.sh
```

## 七、遗留与说明

1. **验收门已消费**：P0×3 修复后 start-app.sh 已去除签名/wall 偏差覆盖，出厂等价实例全量复跑绿（§八第二批）；后续任何签名/数据源配置变更，复跑 §六 第 2、3 步即可回归验收。
2. IT 深覆盖不重复项：SKIP LOCKED 并发/reaper/多 worker 竞态/限速窗口（NfyDeliveryEngineTest 承载）；SUSPEND 签名面（签名开启环境下由 NfySignatureKeyTest 语义承载）。
3. 本轮 8 个 spec 文件 + 3 个 helper（smtp-sink/pg-fixup/nfy）沉淀于 `frontend/e2e-regression/`，可纳入 CI 常态回归。pg-fixup 已随 ND-L5-01 修复退出使用（保留备查）。
4. 测试轮（09-17）产品代码零改动；修复轮（09-18）改动=缺陷修复本身（§八清单）。

## 八、修复记录（2026-09-18，两批：P1+P2 → P0×3）

### 第一批：P1 + P2×3

| # | 级别 | 修复内容 | 验证 |
|---|---|---|---|
| ND-L5-01 | P1 | `ChannelCoreService.patch`：EMAIL 豁免 lastVerifyAt 前置；EMAIL 熔断态（failCount≥5）显式重启用即清零重置（IM 分支一字不变）。`DeliveryEngine` 投递成功补 `last_verify_at` 置位（对齐「首次投递时校验」自述语义） | 单测 411/411（新增 2 例）· IT 三套件红→绿（NfyChannelFlowTest 补 `email_pending_patch_enabled_succeeds_nd_l5_01`）· l5 spec 去 pg-fixup 走真实 API 使能 · e2e L4\|L5 20/20 |
| D-4 | P2 | 根因修正：app 实际经 framework4j-redis 1.5.1 传递引入 `redisson-spring-boot-starter`（RedissonAutoConfigurationV2/V4 仅 @ConditionalOnClass、端点取 spring.data.redis 默认 6379，与 framework4j.redis 覆盖无关）→ app yml `spring.autoconfigure.exclude` 排除两者 + 数据源级 `redisson.enabled: false` 作框架语义声明 | 重启后日志 0 次 6379/redisson 连接（修复前 25 连接）；Lettuce 26379 正常 |
| F-1 | P2 | 新增 `frontend/src/utils/fwkTime.ts`（`parseFwkTime`：数字字符串→Number→Date，<1e12 按秒级；ISO 直解；非法→null），Messages/Announcements/Bell/Deliveries 四页解析点全部改走；API 类型放宽 `number\|string\|null` | vitest 382/382（新增 24）· 三门全绿 · L8 截图确认渲染真实日期 |
| F-2 | P2 | `client.ts` 新增 `onAuthExpired`（触发条件收窄：HTTP 401 或信封 code∈[10200,10300)，排除业务 403 域闸）→ `nfyContext` 接线 `authError` + `handshake.notifyExpired()`（嵌入态回 waiting 重握手，非嵌入回 idle）→ Shell/消息/公告页呈现「会话已失效，正在重新连接…」而非误导空态；BellPage 同接自愈 | vitest（红→绿 24 例）· L8-08 spec 翻转为新行为 · 全量 e2e 复验 |

### 第二批：P0×3（出厂等价验收）

| # | 级别 | 修复内容 | 验证 |
|---|---|---|---|
| D-2 | P0 | `NfyAutoConfiguration` 类加 `@AutoConfigureBefore(name="fun.commons.framework4j.signature.config.SignatureAutoConfiguration")`（FQCN 经 m2 sources jar 核实；fwk4j 兜底 Bean 为按类型 @ConditionalOnMissingBean，排序后自动让位）；`nfyTenantSecretProvider` 移除 `@ConditionalOnMissingBean(SecretProvider.class)`（本项目 provider 即确定性实现） | 新增 NfySignatureFaceTest（signature.enabled+patterns 下：未签名→10101 钉面生效 / 真实租户签名→code=0 钉接线）：修前红（10200）→修后绿 2/2；启动日志 InMemorySecretProvider 出现 0 次 |
| D-1 | P0 | app yml `framework4j.signature.path-patterns: []`（原两个 runtime pattern 移入注释作 S2S 加回示例；enabled:true 基础设施保留）——SPA 与 S2S 共用 runtime 路径，出厂摘除强制面即架构内唯一自洽解；接口设计文档 V1.2.2 修订史同步 | 出厂等价实例（start-app.sh 已无签名覆盖）全量 e2e **70/70**：SPA 未签名直调 runtime 全通即活体验证 |
| D-3 | P0 | app yml druid 显式 `filters: stat,slf4j` + 注释（wall 不兼容 PG 方言：SKIP LOCKED 的 FOR UPDATE OF / Flyway 部分索引谓词） | 出厂等价实例 `sql injection violation`=0（修复前 40 秒 32 次）；引擎全链 e2e 绿 |

**修复后总验收（出厂等价实例）**：starter 单测 **411/411**（双 jacoco check 满足）· 全量 IT **129/129**（八线 RC=0，较基线 +2 签名面用例）· 全量 e2e **70/70** · fat jar 重建并字节级核对（NfyAutoConfiguration 排序注解、yml filters/patterns 均在产）· 日志三零：InMemorySecretProvider=0、sql injection violation=0、127.0.0.1:6379=0。

**当前开放缺陷：无**（P3 观察项 8 条见 §四，均为体验/文档/测试基建类，不阻塞功能）。
